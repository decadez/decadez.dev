## Outputs

The package ships compiled JavaScript, declarations, and a single stylesheet entry. Consumers import components from `@decadez/web-dev-ui` and styles from `@decadez/web-dev-ui/styles.css`.
